<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ecolink"
        tools:targetApi="31">
        <activity
            android:name=".ui.LandingPageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <!-- Add any intent filters or other activity-related configurations here -->
        </activity>
        <activity
            android:name=".ui.SignupActivity"
            android:exported="true">
            <!-- Add any intent filters or other activity-related configurations here -->
        </activity>

        <activity
            android:name=".ui.UserProfileActivity"
            android:exported="true">
            <!-- Add any intent filters or other activity-related configurations here -->
        </activity>
        <activity
            android:name=".ui.ModifyProfileActivity"
            android:exported="true">
            <!-- Add any intent filters or other activity-related configurations here -->
        </activity>
        <activity android:name=".ui.DeactivatedUserActivity" />
        <activity android:name=".ui.BannedUserActivity" />
        <activity
            android:name=".ui.DeactivateAccountActivity"
            android:exported="true">
            <!-- Add any intent filters or other activity-related configurations here -->
        </activity>

        <activity
            android:name=".ui.PinEntryActivity"
            android:exported="true">
            <!-- Add any intent filters or other activity-related configurations here -->
        </activity>
        <activity
            android:name=".ui.PasswordResetActivity"
            android:exported="true">
            <!-- Add any intent filters or other activity-related configurations here -->
        </activity>
    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>